version: '3.8'

services:
  bizgenesis:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - SERPER_API_KEY=${SERPER_API_KEY}
    volumes:
      - ./.env:/app/.env
    restart: unless-stopped
